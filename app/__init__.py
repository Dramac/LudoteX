"""Paquet applicatif `app` : backend FastAPI du système de prêt de jeux.

Sous-modules clés : models (schéma SQLite), db (connexion/init), services
(logique métier), auth (jeton bénévole), templating (Jinja2), main (assemblage),
et le sous-paquet routes/ (une route par domaine fonctionnel).
"""
